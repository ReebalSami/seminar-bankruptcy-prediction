            </div>
            
            <div style="margin-top: 40px; padding-top: 20px; border-top: 1px solid #ddd; color: #7f8c8d; font-size: 0.9em;">
                <p>Generated: {pd.Timestamp.now().strftime('%Y-%m-%d %H:%M:%S')}</p>
                <p>Project: Bankruptcy Prediction Seminar - Phase 04c: Embedded Methods</p>
            </div>
        </div>
    </body>
    </html>
    """
    
    with open(output_path, "w", encoding="utf-8") as f:
        f.write(html_content)


# =============================================================================
# Main Execution
# =============================================================================

def main():
    """Main execution function."""
    logger.info("="*80)
    logger.info("PHASE 04c: EMBEDDED METHODS FOR FEATURE SELECTION")
    logger.info("="*80)
    
    # Load imputed dataset
    logger.info(f"\nLoading dataset: {DATA_PATH}")
    df = pd.read_parquet(DATA_PATH)
    logger.info(f"  ✓ Loaded {len(df):,} observations")
    
    # Process each horizon
    all_results = {}
    for horizon in HORIZONS:
        results = process_horizon_embedded(horizon, df)
        save_horizon_results(horizon, results)
        all_results[f"H{horizon}"] = results
    
    # Consolidated summary
    logger.info("\n" + "="*80)
    logger.info("PHASE 04c COMPLETE")
    logger.info("="*80)
    logger.info(f"Results saved to: {RESULTS_DIR}")
    logger.info("\nNext step: Run 04d_stability_analysis.py")


if __name__ == "__main__":
    main()
